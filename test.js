/* eslint-env mocha */
const assert = require('assert');
const mountHtml = require('./index');

describe('MountHtml', function () {
  // it('should override objects', function () {
  //   const a = {
  //     foo: 'a'
  //   };
  //   const b = {
  //     foo: 'b'
  //   };

  //   assert.deepEqual(merge(a, b), b);
  // });

});
